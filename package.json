{
  "name": "codex-tg-bridge",
  "version": "0.1.1",
  "description": "Zero-degradation Telegram bridge to OpenAI Codex via app-server JSON-RPC over WebSocket. Bridge forwards raw user text to Codex without injecting prompts or overriding config; streams item/* notifications back to TG unchanged.",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun src/server.ts",
    "start-gemini": "bun install --no-summary && bun src/gemini-server.ts",
    "typecheck": "bun tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "grammy": "^1.32.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
